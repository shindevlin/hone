[package]
name = "btcpc-types"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "BTCPC protocol types — block format, ledger entries, account state"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
borsh = { version = "1", features = ["derive"] }
sha2 = "0.10"
hex = "0.4"
