node_modules/
.env
telegram-bot/.env
coverage/
data/blocks/
data/pending-entries.jsonl

# Docker image tarball (hosted at btcpc.net, too large for git)
website/btcpc-image.tar.gz
website/btcpc-image.tar

# Obsidian vault config — open docs/ as a vault, exclude its workspace
.obsidian/
docs/.obsidian/

# Local tool configs (not project code)
.claire/
.mcp.json

# Forge build artifacts
out/
cache/

# Added by code-review-graph
.code-review-graph/
