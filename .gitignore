node_modules/
**/node_modules/
.env
telegram-bot/.env

# Wallet and private key files — NEVER commit these
*.wallet.key
wallet.key
key.json
secrets.json
BTCPC_Keys/
coverage/
data/blocks/
data/pending-entries.jsonl

# Docker image tarball (hosted at btcpc.net, too large for git)
website/btcpc-image.tar.gz
website/btcpc-image.tar

# Obsidian vault config — open docs/ as a vault, exclude its workspace
.obsidian/
docs/.obsidian/

# Local tool configs (not project code)
.claire/
.mcp.json
.claude/worktrees/
.ufbt-venv/
.ufbt-tools/
.btcpc-image-hash

# Forge build artifacts
out/
cache/

# Added by code-review-graph
.code-review-graph/

# Build outputs — distributed via GitHub Releases, not repo
website/BTCPC-android-release.apk
website/BTCPC-desktop-linux.AppImage
website/btcpc-desktop-linux.deb
android/btcpc-release.keystore
android/app/build/

# Rust build artifacts
rust/*/target/
rust/target/
android/rust/*/target/

# ORT vendored patch (large binary, not for git)
android/rust/ort-patched/

# Local cargo config (machine-specific)
rust/btcpc-node/.cargo/

# Proptest regression files (generated by tests)
rust/btcpc-node/proptest-regressions/
rust/*/proptest-regressions/

# Generated cargo docs
rust/btcpc-sdk/doc/
rust/*/doc/

# Runtime chain data (changes constantly, not source)
data/known-peers.json
data/seen-messages.json
data/anchors/

# Temp / scratch files
929123

# Large standalone projects (separate repos / too big)
/camofox-browser/
/btcpc/

# Website redesign WIP (not ready for main)
website-redesign/

# Scheduled task lock
.claude/scheduled_tasks.lock

# Graphify knowledge graph output (generated, not source)
graphify-out/
