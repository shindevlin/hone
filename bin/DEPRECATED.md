# DEPRECATED — Node.js CLI binaries

`hone-mine` and `hone-cli` in this directory are the legacy Node.js mining daemon and wallet CLI.

**They are not used. Do not run them.**

The active node binary is built from `rust/hone-node/` and managed via systemd as `hone-node`.
