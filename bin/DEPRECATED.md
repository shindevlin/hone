# DEPRECATED — Node.js CLI binaries

`btcpc-mine` and `btcpc-cli` in this directory are the legacy Node.js mining daemon and wallet CLI.

**They are not used. Do not run them.**

The active node binary is built from `rust/btcpc-node/` and managed via systemd as `btcpc-node`.
