# freeport

Peer-to-peer port routing for BTCPC nodes — lets nodes behind NAT/firewalls
expose services without static IPs or port forwarding. Uses the DHT (btcpc-p2p)
to negotiate tunnels between peers.

## Status

Design phase. Companion to `rust/btcpc-p2p`.
