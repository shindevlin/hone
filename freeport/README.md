# freeport

Peer-to-peer port routing for HONE nodes — lets nodes behind NAT/firewalls
expose services without static IPs or port forwarding. Uses the DHT (hone-p2p)
to negotiate tunnels between peers.

## Status

Design phase. Companion to `rust/hone-p2p`.
